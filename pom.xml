<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.example</groupId>
  <artifactId>village-toolkit</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- J2ME API stub jars, published on Maven Central by the MicroEmulator
         project - these give javac the class signatures to compile against.
         They are compile-time stubs only; nothing from them ships on the
         phone, so this has no bearing on what your actual T528 supports. -->
    <microemu.version>2.0.4</microemu.version>
    <j2me.finalName>Toolkit</j2me.finalName>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.microemu</groupId>
      <artifactId>midpapi20</artifactId>
      <version>${microemu.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.microemu</groupId>
      <artifactId>cldcapi11</artifactId>
      <version>${microemu.version}</version>
      <scope>provided</scope>
    </dependency>
    <!-- Compile-time classes for SoundManager.java's javax.microedition.media
         calls. This is the one dependency in this file I could not fully
         verify - if the build fails specifically on SoundManager.java,
         paste me the error and I'll adjust; the fallback is simply deleting
         SoundManager.java and the two sound-related lines in
         ToolkitCanvas.java, since sound was designed to be fully optional. -->
    <dependency>
      <groupId>org.microemu</groupId>
      <artifactId>microemu-jsr-135</artifactId>
      <version>${microemu.version}</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>${project.artifactId}</finalName>
    <plugins>

      <!-- 1. Copy the API stub jars into target/j2me-libs so we can point
              javac's -bootclasspath at them directly. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>3.11.0</version>
        <executions>
          <execution>
            <id>copy-j2me-api</id>
            <phase>generate-sources</phase>
            <goals><goal>copy</goal></goals>
            <configuration>
              <artifactItems>
                <artifactItem>
                  <groupId>org.microemu</groupId>
                  <artifactId>midpapi20</artifactId>
                  <version>${microemu.version}</version>
                  <outputDirectory>${project.build.directory}/j2me-libs</outputDirectory>
                  <destFileName>midpapi20.jar</destFileName>
                </artifactItem>
                <artifactItem>
                  <groupId>org.microemu</groupId>
                  <artifactId>cldcapi11</artifactId>
                  <version>${microemu.version}</version>
                  <outputDirectory>${project.build.directory}/j2me-libs</outputDirectory>
                  <destFileName>cldcapi11.jar</destFileName>
                </artifactItem>
                <artifactItem>
                  <groupId>org.microemu</groupId>
                  <artifactId>microemu-jsr-135</artifactId>
                  <version>${microemu.version}</version>
                  <outputDirectory>${project.build.directory}/j2me-libs</outputDirectory>
                  <destFileName>mmapi.jar</destFileName>
                </artifactItem>
              </artifactItems>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- 2. Compile against those stubs, targeting old bytecode a J2ME
              phone's verifier expects. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.14.0</version>
        <configuration>
          <source>1.3</source>
          <target>1.3</target>
          <compilerArgs>
            <arg>-bootclasspath</arg>
            <arg>${project.build.directory}/j2me-libs/midpapi20.jar${path.separator}${project.build.directory}/j2me-libs/cldcapi11.jar${path.separator}${project.build.directory}/j2me-libs/mmapi.jar</arg>
            <arg>-Xlint:-options</arg>
          </compilerArgs>
        </configuration>
      </plugin>

      <!-- 3. Package the .class files with our manifest.mf, so MIDlet-1
              (including the icon path) is set correctly inside the JAR. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>3.4.2</version>
        <configuration>
          <archive>
            <manifestFile>${project.basedir}/manifest.mf</manifestFile>
          </archive>
        </configuration>
      </plugin>

      <!-- 4. Preverify the bytecode via ProGuard's "-microedition" mode -
              this replaces the old standalone `preverify` tool, which has
              no clean way to run in CI. dontoptimize/obfuscate=false keep
              the class names intact, since manifest.mf references
              "ToolkitMIDlet" by its literal name. -->
      <plugin>
        <groupId>com.github.wvengen</groupId>
        <artifactId>proguard-maven-plugin</artifactId>
        <version>2.7.0</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals><goal>proguard</goal></goals>
          </execution>
        </executions>
        <configuration>
          <injar>${project.build.finalName}.jar</injar>
          <outjar>${j2me.finalName}.jar</outjar>
          <includeDependency>true</includeDependency>
          <obfuscate>false</obfuscate>
          <options>
            <option>-dontoptimize</option>
            <option>-microedition</option>
            <option>-target 1.3</option>
            <option>-keep public class * extends javax.microedition.midlet.MIDlet</option>
            <option>-dontnote</option>
            <option>-dontwarn</option>
          </options>
        </configuration>
      </plugin>

      <!-- 5. Generate the .jad by reusing manifest.mf as its base and
              appending the jar's URL and real byte size, then collect both
              final files into target/j2me/. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <version>3.1.0</version>
        <executions>
          <execution>
            <id>j2me-artifacts</id>
            <phase>package</phase>
            <goals><goal>run</goal></goals>
            <configuration>
              <target>
                <mkdir dir="${project.build.directory}/j2me"/>
                <copy file="${project.build.directory}/${j2me.finalName}.jar"
                      tofile="${project.build.directory}/j2me/${j2me.finalName}.jar"/>
                <length file="${project.build.directory}/j2me/${j2me.finalName}.jar"
                        property="midlet.jar.size"/>
                <copy file="${project.basedir}/manifest.mf"
                      tofile="${project.build.directory}/j2me/${j2me.finalName}.jad"/>
                <echo file="${project.build.directory}/j2me/${j2me.finalName}.jad" append="true">MIDlet-Jar-URL: ${j2me.finalName}.jar
MIDlet-Jar-Size: ${midlet.jar.size}
</echo>
              </target>
            </configuration>
          </execution>
        </executions>
      </plugin>

    </plugins>
  </build>
</project>
